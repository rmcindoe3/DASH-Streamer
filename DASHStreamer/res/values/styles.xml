<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="android:background">@color/bg_color</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
    </style>
    
    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse" >
        <item name="android:background">@drawable/action_bar_bg</item>
    </style>
    
    <style name="AppTheme.Layout">
        <item name="android:background">@drawable/activity_bg</item>
    </style>
    
    <style name="AppTheme.Button">
    </style>

    <style name="AppTheme.Button.Navigation">
        <item name="android:background">@drawable/selector_navigation_button</item>
        <item name="android:padding">@dimen/navigation_button_padding</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
